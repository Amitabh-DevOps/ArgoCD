apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: team-backend
  namespace: argocd
  labels:
    team: backend
spec:
  description: "Project for the Backend team — isolated to backend namespace and repo"

  # ── Allowed source repositories ──────────────────────────────────────
  sourceRepos:
    - https://github.com/Amitabh-DevOps/ArgoCD.git

  # ── Allowed destination clusters and namespaces ──────────────────────
  destinations:
    - server: https://kubernetes.default.svc
      namespace: backend

  # ── Cluster resource allow list ──────────────────────────────────────
  clusterResourceWhitelist:
    - group: ""
      kind: Namespace

  # ── Namespace resource allow list ────────────────────────────────────
  namespaceResourceWhitelist:
    - group: "apps"
      kind: Deployment
    - group: ""
      kind: Service
    - group: ""
      kind: ConfigMap
    - group: ""
      kind: Secret
    - group: ""
      kind: ServiceAccount

  # ── RBAC roles scoped to this project ────────────────────────────────
  # Note: 'groups' here is for SSO group mapping (covered in SSO session).
  # For local users, mapping is done in argocd-rbac-cm.yaml using:
  #   g, bob, proj:team-backend:backend-admin
  roles:
    - name: backend-admin
      description: Full access to backend project apps
      policies:
        - p, proj:team-backend:backend-admin, applications, *, team-backend/*, allow
        - p, proj:team-backend:backend-admin, logs, get, team-backend/*, allow

    - name: backend-readonly
      description: Read-only access to backend project
      policies:
        - p, proj:team-backend:backend-readonly, applications, get, team-backend/*, allow
