apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: wave-demo
  annotations:
    argocd.argoproj.io/sync-wave: "0"    # ← deployed SECOND (after namespace)
data:
  APP_ENV: "production"
  APP_PORT: "3000"
  APP_NAME: "argocd-demo"
